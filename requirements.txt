# frlg-ldn-trade runtime dependencies (tested versions; install into a venv)
# ldn is pinned to our fork's feature/kernel-ccmp-encryption branch: it adds
# CreateNetworkParam.skip_encryption (delegated mac80211/hardware CCMP for the
# host's mt7601u setup) on top of kinnay/LDN's upstream.
ldn @ git+https://github.com/MercuryEnigma/LDN.git@feature/kernel-ccmp-encryption
pycryptodome==3.23.0
trio==0.33.0
zstandard==0.25.0
